@{
    ViewBag.Title = "Error";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center mt-5">
    <h1 class="text-danger">¡Ocurrió un error!</h1>
    <h4 class="text-muted">No se pudo procesar la solicitud en este momento.</h4>
    <p class="mt-3">
        Puedes volver al <a href="@Url.Action("Index", "Home")">inicio</a> o contactar al administrador si el problema persiste.
    </p>
</div>
