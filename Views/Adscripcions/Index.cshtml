@model IEnumerable<pactica123.Models.Adscripcion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre_adscripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ubicacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_adscripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ubicacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_adscripcion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_adscripcion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_adscripcion })
        </td>
    </tr>
}

</table>
